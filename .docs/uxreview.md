### Bottom Nav Bar
Fix 1: on the nav bar, can the library icon be the musicnote icon

### Library Tabs/Pages
Fix 1: there is a divider above the tabs and the active tab stroke is below the tabs, change the stroke to be on top of the active tab
Fix 2: The Library and Downloads filter chips, what is a nicer interaction here and on the Search page?  
Fix 3: Remove the user icon avatar in the upper-right corner
Fix 4: what is the debug banner at the top-right ? remove it
Fix 5: Have the Search Library field by default at the top, similar to the Search Page , so remove the 'Library' header, the search icon, and the avatar icon and just have the search field by default. If we do this, then do we even need the search page/tab? can just deafult the filter chips to 'Library' and when I search I can switch to 'All Music' if I want - the best part is no part. So then it is just Music, Queue, and Settings tabs? Futureproof this as we integrate youtube music search for example. And if I go to the search field, and tab 'All Music' then display the Search for Music empty state like on the current search page

Songs, Albums,  and Playlist landing pages are ok (item rows)
Fix 6: the Artist rows, remove the 'Artist' label next to the more menu

### Queue Page
Fix 1: The grip icons have different colors, please fix
Fix 2: On Queued song row items, in the more menu, are those the right options? what about Add to Library/Remove from Library, Add to Playlist, Remove from Queue, Download, Play Next, Share, Start Radio, View Album, View Artist? Add those missing items
Fix 3: Now Playing Song section, the lyrics, we probably won't have those so remove it, and the 2 buttons then should be Add to Playlist and More
Fix 4: Now Playing Song section, make the skip and rewind buttons have circle outlines too please
Fix 5: Now Playing Section, more menu, options should be Add to Playlist, Download, Share, Start Radio, View Album, View Artist

### Artist Page/Screen
Fix 1: Need to flip the badges and tabs, at bottom is Songs, Albums as tabs
Fix 2: All Music, Library, Downloads are filter chips
Fix 3: the buttons at the top do nothing right now
Fix 4: For All Sort A-Z by default
Fix 5: how do we plan for youtube music integration for videos? is that another tab next to albums? 

### Playlist Page/Screen
Fix 1: Missing icon buttons for Edit (playlist), Download, and Remove/Delete in the top header below the song count
Fix 2: Missing the Play, Shuffle, and Add to Queue buttons
Fix 3: Clicking 'Edit' I see the song item rows change so there is a grip icon on the left to change the playlist order and a remove/trash icon on the right to remove a song, clicking the trash can icon triggers the item to animate into a 'Confirm delete/remove' button instead of modal or other double confirmation

### Album Page/Screen
Fix 1: The second row of buttons should be icon left label right for Play, Shuffle, and Queue
Fix 2: Icon buttons below the year and song count should only be Add to Library/Remove from Library and Download

### Overall
Fix 1: The download icon buttons next to song items and others should have an animation that change from 'download for offline' icon to 'download done' icon